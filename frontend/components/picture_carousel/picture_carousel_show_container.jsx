import { connect } from 'react-redux';
import PictureCarouselShow from './picture_carousel_show';

const mapStateToProps = state => {

    return ({

    });
};

const mapDispatchToProps = dispatch => {

    return ({

    });
};

export default connect (mapStateToProps, mapDispatchToProps)(PictureCarouselShow);